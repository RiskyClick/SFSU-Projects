#sum -> $s0
#100 -> $t0
#i -> $t1
	li 	$s0, 0
	li 	$t1, 1
	li 	$t0, 100

loop:	add	$s0, $s0, $t1
	addi	$t1, $t1, 1
	ble	$t1, $t0, loop


int x[] = {1, 2, 3, 4, ... 8}
int sum = 0
int size = 8
for (int i = 0; i < size; i++){
	sum += x[i];
}
cout << "sum " << sum << endl;
# x -> $s0
# sum -> $s1
# size -> $s2
# i -> $t0

	li 	$s1, 0
	li	$s2, 8
	li	$t0, 0
	la	$s0, x

	bge	$t0, $s2, exit
loop:	lw	$t1, ($s0)
	add	$s1, $s1, $t1
	addi	$s0, $s0, 4
	blt	$t0, $s2, loop
exit:
	cout in mips
	